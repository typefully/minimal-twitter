const Container = ({ children }) => (
  <div className="relative flex flex-col p-1 font-sans dark:text-white text-black font-normal max-w-full w-[420px] w-full">
    {children}
  </div>
)

export default Container
